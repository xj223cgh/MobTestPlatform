# 平台访问与 Agent 流程说明

适用于：**平台部署在服务器**、多名测试人员用**浏览器**访问、设备可能接在**各用户本机**的场景。

## 架构要点

```
平台服务器：前端(8081) + 后端(5000) + MySQL
     │
     ├─ 用户 A：浏览器 ── 可选：本机运行 Agent（USB 设备接在 A 的电脑）
     ├─ 用户 B：同上
     └─ 用户 C：仅浏览器（未绑 Agent 时列表来自服务器本机 adb）
```

- **Agent** 跑在用户**当前操作电脑**上，通过 Socket.IO 连接平台后端，负责该机的 `adb`/投屏指令。
- Agent 与平台后端的通信必须指向**服务器后端地址**，而非 `127.0.0.1`（除非平台也在本机）。

## 设备列表来源（核心规则）

| 条件 | 设备列表来自 |
|------|----------------|
| 当前用户已绑定本机 Agent，且 Agent **在线** | Agent 所在机器的 `adb devices` |
| 未绑定或 Agent **离线** | **服务器本机**的 `adb devices` |

同一用户**同一时间**只绑定一台 Agent；在新电脑运行 Agent 并再次绑定会覆盖原绑定。

## Agent 注册与绑定流程

1. **注册**：Agent 启动时向平台后端注册，获取 `agent_uid` 和 `token`，持久化到本地 `agent_config.txt`。
2. **ADB 接管**：注册完成后，Agent 自动用内置 ADB 执行 `kill-server` + `start-server`，接管本机 ADB Server，避免与用户已安装的其他版本 ADB 冲突（多版本共存会导致设备列表闪烁或命令超时）。
3. **绑定**：用户在设备管理页点击「绑定本机」，平台检测本机 Agent（`127.0.0.1:8765`）自动完成绑定；若未检测到，生成 6 位绑定码供手动绑定。
4. **心跳**：Agent 定期上报在线状态，后端据此判断 Agent 是否可用。
5. **解绑**：设备管理页解绑或 Agent 清理时自动解绑。

## 部署与下载

1. 服务器部署 `backend`、`frontend`，`agent/dist/` 下放置 `MobTestAgent.exe`，供用户从页面下载。
2. 启动：`python start.py` 或分别启动后端与前端；防火墙/反代放行 5000、8081。

## 用户侧：Agent 指向服务器

Agent 默认连 `http://127.0.0.1:5000`；平台在服务器上时必须改为服务器后端，例如：

```text
set AGENT_BASE_URL=http://192.168.1.100:5000
MobTestAgent.exe
```

或：`MobTestAgent.exe --base-url http://192.168.1.100:5000`

前端「一键绑定 / 自动绑定」是浏览器与本机 Agent（`127.0.0.1:8765`）交互；**Agent 与平台的业务连接始终是 Agent → 服务器后端**。

## 多用户小结

| 问题 | 结论 |
|------|------|
| 多人同时用同一平台？ | 可以；账号与 Agent 绑定相互独立。 |
| 设备列表不一致？ | 正常：每人看到的是「自己的 Agent 机」或「服务器 adb」。 |
| 设备接在服务器上？ | 不绑 Agent 的用户会看到服务器本机设备。 |

## 相关文档

- Agent 使用方法与打包：[agent/README.md](../../agent/README.md)
- 内网访问与防火墙：[内网访问说明.md](./内网访问说明.md)
